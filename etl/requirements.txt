# ---------------------------------------------------------------------------
# Dependências Python do Projeto Integrador — Área 03 (Banco de Dados)
#
# Instalar:
#   python3 -m venv .venv
#   source .venv/bin/activate
#   pip install -r etl/requirements.txt
#
# Versões FIXADAS (==) de propósito: reprodutibilidade é critério de nota.
# Um avaliador que rodar este arquivo daqui a seis meses instala exatamente
# os mesmos pacotes que foram usados para gerar os resultados do relatório.
#
# São quatro dependências diretas, todas drivers ou ferramentas de análise.
# Nenhum framework que esconda o trabalho de modelagem ou de consulta:
#
#   psycopg[binary] .. driver oficial do PostgreSQL para Python (versão 3).
#                      O extra [binary] traz o libpq compilado junto, evitando
#                      ter de instalar bibliotecas de sistema no Ubuntu.
#                      Usado pelo ETL (public -> nw -> MongoDB) e pelo benchmark.
#   pymongo ......... driver oficial do MongoDB. Mesmo papel do psycopg, do
#                      outro lado da comparação.
#   tabulate ........ formata resultados de consulta como tabela de texto ou
#                      Markdown. É o que transforma uma saída de consulta em
#                      evidência apresentável em apresentacao/evidencias/.
#   matplotlib ...... gráficos (.png) do benchmark e da análise exploratória.
#
# PDF, PPTX, DOCX e XLSX NÃO entram aqui: a geração dos documentos das
# entregas é feita fora deste ambiente.
# ---------------------------------------------------------------------------

# --- dependências diretas --------------------------------------------------
matplotlib==3.11.1
psycopg==3.3.4
pymongo==4.17.0
tabulate==0.10.0

# --- transitivas (fixadas para o ambiente ficar idêntico) ------------------
contourpy==1.3.3
cycler==0.12.1
dnspython==2.8.0
fonttools==4.63.0
kiwisolver==1.5.0
numpy==2.5.2
packaging==26.3
pillow==12.3.0
psycopg-binary==3.3.4
pyparsing==3.3.2
python-dateutil==2.9.0.post0
six==1.17.0
typing_extensions==4.16.0
